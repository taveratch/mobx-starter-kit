apiVersion: v1
kind: Service
metadata:
  name: mobx-starter-kit-service
  namespace: default
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: mobx-starter-kit